---

- hosts: all
  become: true
  tasks:

  - name: update repo index
    apt:
      update_cache: yes

  - name: install nginx package
    apt:
      name: nginx
      state: absent

  - name: resource monitor
    apt:
      name: htop
      state: absent
